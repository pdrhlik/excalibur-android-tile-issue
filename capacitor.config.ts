import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'excalibur-android-tile-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
